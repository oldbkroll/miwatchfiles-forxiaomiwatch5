# M2 启动与文本查看/编辑 closeout

日期：2026-07-19

状态：本地实现和自动化测试完成；真机文本验收因本次无线 ADB 无在线设备而未完成。不能据此声称 M2 真机通过。

## 已交付范围

- 启动与目录只读基线已记录；基线期间没有创建、删除或修改真机文件，也没有发现足以支持盲目启动优化的可定位瓶颈。
- 简单纯文本使用严格 UTF-8 解码，按约 32 KiB 安全边界分段查看；查看上限 16 MiB，编辑上限 512 KiB，超限文本保持只读。
- 支持 `.txt`、`.text`、`.log`、`.csv`、`.md`；`.txt`、`.text`、`.log` 映射为 `text/plain`，未知类型仍保留原有“用其他应用打开”。
- 文本查看、分页、编辑草稿、覆盖保存、当前目录内另存为、同名确认、取消/放弃编辑和失败提示均接入圆屏页面。
- 覆盖和另存为共用同一安全写入模块：摘要校验、当前目录约束、符号链接拒绝、同目录唯一临时文件、强制落盘、backup、发布后校验、失败回滚和应用启动/文本操作前事务恢复。
- 保持 `targetSdk 29`、`requestLegacyExternalStorage`、`armeabi-v7a` 和小米自定义表冠滚动逻辑；没有新增播放器、音频组件、缩略图缓存或大型依赖。

## 自动化证据

以下命令在项目根目录执行并成功：

| 命令 | 结果 |
|---|---|
| `:app:compileDebugKotlin` | PASS |
| `:app:testDebugUnitTest --tests 'com.example.watchfiles.text.*' --tests 'com.example.watchfiles.browser.*'` | PASS |
| `:app:testDebugUnitTest` | PASS |
| `:app:assembleDebug` | PASS |

Debug APK：`app/build/outputs/apk/debug/app-debug.apk`

SHA-256：`B124A2522ED79204DBC8233DC7F1FBC411AE4E6DE7BB10CCD73560804DD3261F`
包名：`com.example.watchfiles.debug`；`versionCode=6`；`targetSdk=29`。

自动化覆盖包括：文本扩展名/大小边界、空文件、换行、Unicode 多字节分页边界、非法 UTF-8、编辑状态流、保存摘要冲突、覆盖/另存为同名确认、写入各故障点、取消、事务恢复和自有临时文件清理。

## 真机证据与边界

### 已完成的启动基线

启动基线使用当时动态发现的 mDNS serial：
`adb-d87a2e34-S40wiQ._adb-tls-connect._tcp`；当时 mDNS 地址为 `192.168.31.60:41615`，未使用旧的 `192.168.31.60:34913`。实际 Activity 组件为 `com.example.watchfiles.debug/com.example.watchfiles.MainActivity`。

- 冷启动 5 次 `TotalTime`：3668、3751、3604、3589、3617 ms；中位数 3617 ms，最慢 3751 ms。
- 热启动 5 次 `TotalTime`：87、82、86、77、85 ms；中位数 85 ms，最慢 87 ms。
- 主页进入内部存储首帧代理：950、1008、885、867、943 ms；中位数 943 ms，最慢 1008 ms。
- 主页/目录/图片页 PSS 以及连续图片进入/退出 5 轮的原始值见 [`2026-07-19-m2-startup-baseline.md`](2026-07-19-m2-startup-baseline.md)。

### 未完成的当前会话验收

本次文本验收前按要求重新执行了：

```text
adb devices -l
adb mdns services
```

初次结果显示 `192.168.31.60:41615` 和 mDNS serial 均为 `offline`，没有在线 serial；重启本地 ADB daemon 后再次执行，设备列表和 mDNS 服务仍为空。因此未安装本次 Debug APK，未启动页面，未读取或写入 `/storage/emulated/0/Download/WatchFilesTest/M1Sandbox`，未运行真机 logcat/PSS/crash audit。没有使用历史无线地址，也没有执行任何真机写入替代操作。

待设备重新在线后，必须在一个新的设备会话中重新动态发现 serial，并仅在 M1Sandbox 内完成：只读文本查看、分页、超限只读和非法编码提示；覆盖/另存为正常成功、取消、同名确认；源/目标 SHA-256、`.part`/`.backup` 清理以及 `AndroidRuntime`/`FATAL EXCEPTION` 审计。

## 路线图决定

由于本 closeout 没有新的真机文本查看和真实文件写入证据，`PROJECT_PLAN.md` 中 M2 的“启动速度与基础性能基线及优化”“分段纯文本查看”“简易文本编辑”仍保持未勾选。代码和本地测试已准备好下一次设备会话，不能把本地 PASS 写成真机通过。
