# WatchFiles 路线图

## M3：可靠性、后台操作与性能收尾

- [x] 前台文件操作服务（本地 gates 与真机 COPY/MOVE/DELETE 冒烟已通过）
- [x] 大任务亮屏风险提示（实现、单元测试、本地 Debug gate 和 Watch 5 真机回归已完成；递归项目数 ≥ 100 或已知总容量 ≥ 52,428,800 bytes〔50 MiB〕时，执行前提示用户尽量保持亮屏）
- [x] 启动、内存和目录加载性能收尾（Watch 5 当前 Debug 只读回归：冷启动中位数 3595 ms；目录层级、文本查看、图片预览和选择交互正常；图片预览退出后 PSS 未持续失控；应用进程错误审计为空）
- [x] 表冠交互和触觉反馈的厂商兼容实现（自定义表冠队列、无连续滚动震动、顶部/底部各一次短反馈、长按选择和 Watch 5 只读回归已完成）

M3 真机记录见 `docs/context/m3-foreground-file-operation-service-closeout.md`。2026-07-24 用户确认在线 ADB 设备为目标 Watch 5；当前构建已完成大任务 COPY/MOVE/DELETE 提醒、取消/返回、DELETE 二次确认链和普通小任务 no-warning 回归。真机写入仍限于 M1Sandbox。

表冠交互与触觉代码已完成本地实现、JVM 测试、Debug 构建和 Lint；当前动态发现的 Watch 5 已完成正常滚动无连续震动、顶部/底部各一次短反馈、长按选择、平台触觉记录和崩溃审计。触觉强度未做主观量化，不改变本项完成结论。

2026-07-24 已完成 M3 性能收尾只读复测：当前动态发现的 `M2505W1` / `grasslte`（Android 14 / API 34）冷启动 5 次为
`3617/3580/3593/3595/3596 ms`，中位数 `3595 ms`；主页、内部存储、Download、WatchFilesTest、M1Sandbox、文件详情、UTF-8 文本查看、长按选择和
4000×3000 图片低内存预览均可用。图片预览 5 轮首尾 PSS 增量约 `1.6 MiB`，退出预览回到详情为 `72699 KiB`；清空日志后的最终导航审计没有匹配到应用进程错误，M1Sandbox 顶层清单未变化。详细数据见
`docs/superpowers/checkpoints/2026-07-24-m3-performance-closeout.md`。

### M3 当前边界调整

- 不实现进程终止后的操作恢复、恢复日志、自动重试或任务持久化；`START_NOT_STICKY` 保持为明确边界。
- 不把熄屏继续操作或 Activity 重入后的任务恢复作为手表产品能力验收；不强制保持应用亮屏或改变系统调度策略。
- 不执行为凑阈值而构造的 `100 项`、`50 MiB` 或 `5000 项` 真机压力测试；以大任务执行前的亮屏风险提示作为轻量保护措施。

## M4：可选能力

- [ ] 内置视频播放
- [ ] ZIP 查看
- [ ] ZIP 解压
